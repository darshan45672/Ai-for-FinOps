# Environment Configuration for AI-FinOps Application
# Copy this file to .env and update with your values

# Node Environment
NODE_ENV=production

# Database Configuration - Neon PostgreSQL
# Get your connection string from: https://console.neon.tech
DATABASE_URL="postgresql://username:password@your-neon-host.neon.tech/ai_finops?sslmode=require"
DIRECT_URL="postgresql://username:password@your-neon-host.neon.tech/ai_finops?sslmode=require"

# Legacy PostgreSQL Configuration (if using local PostgreSQL)
# POSTGRES_USER=finops
# POSTGRES_PASSWORD=finops_password_change_me
# POSTGRES_DB=ai_finops

# Redis Configuration
REDIS_PASSWORD=redis_password_change_me

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-in-production-min-32-chars
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3003

# API URLs (for frontend)
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_AUTH_URL=http://localhost:3001

# Optional: Additional configurations
# DATABASE_POOL_SIZE=10
# LOG_LEVEL=info

# Generate secure secrets with:
# openssl rand -base64 32
